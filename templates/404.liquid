<div class="section" style="text-align:center;padding:120px 48px;">
  <div style="font-size:5rem;margin-bottom:20px;">🕊️</div>
  <h1 style="font-family:'Cinzel',serif;font-size:clamp(2rem,5vw,3.5rem);color:var(--purple-dark);margin-bottom:16px;">Page Not Found</h1>
  <p style="color:var(--text-muted);max-width:480px;margin:0 auto 36px;font-size:1rem;line-height:1.7;">
    We couldn't find that page, but God knows exactly where everything is. Let's get you back on track.
  </p>
  <div style="display:flex;gap:16px;justify-content:center;flex-wrap:wrap;">
    <a class="btn-primary" href="{{ routes.root_url }}">Go Home</a>
    <a class="btn-secondary" href="{{ routes.all_products_collection_url }}">Shop Now</a>
  </div>
</div>

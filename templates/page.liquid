<div class="page-hero">
  {% render 'breadcrumb', breadcrumb_title: page.title %}
  <h1>{{ page.title }}</h1>
</div>

<div class="section">
  <div class="policy-content">
    {{ page.content }}
  </div>
</div>
